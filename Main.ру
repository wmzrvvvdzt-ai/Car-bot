import asyncio
from aiogram import Bot
from config import TOKEN, CHAT_ID

async def send(msg):
    bot = Bot(token=TOKEN)
    await bot.send_message(CHAT_ID, msg)

async def main():
    await send("Бот запущен 🚗")

asyncio.run(main())
